import { css } from "@emotion/css";

export const appClasses = {
  iconBtn: css({
    display: "inline-flex",
    alignItems: "center",
    justifyContent: "center",
  }),
  spaceVertical: css({
    width: "100%",
  }),
  main: css({
    maxWidth: 620,
    marginLeft: "auto",
    marginRight: "auto",
    padding: "16px",
  }),
};
